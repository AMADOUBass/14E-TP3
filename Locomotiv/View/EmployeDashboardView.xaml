<UserControl
  x:Class="Locomotiv.View.EmployeDashboardView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
>
  <UserControl.Resources>
    <SolidColorBrush x:Key="AccentBrush" Color="#34495E" />
    <SolidColorBrush x:Key="LightBackground" Color="#F4F6F7" />
    <Style TargetType="TextBlock" x:Key="LabelStyle">
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="Margin" Value="0,2,5,0" />
      <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
    </Style>
    <Style TargetType="GroupBox">
      <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
      <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="Background" Value="{StaticResource LightBackground}" />
    </Style>
  </UserControl.Resources>

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <GroupBox Header="Station assignée" Grid.Row="0" Padding="10" Margin="0,0,0,10">
      <StackPanel>
        <TextBlock
          Text="{Binding StationNomAffiche}"
          FontSize="20"
          FontWeight="Bold"
          Foreground="{StaticResource AccentBrush}"
        />

        <TextBlock Text="{Binding StationAssignee.Localisation}" Margin="0,5,0,0" />

        <WrapPanel>
          <TextBlock Text="Type : " Style="{StaticResource LabelStyle}" />
          <TextBlock Text="{Binding StationAssignee.StationType}" />
        </WrapPanel>

        <WrapPanel>
          <TextBlock Text="Capacité max trains : " Style="{StaticResource LabelStyle}" />
          <TextBlock Text="{Binding StationAssignee.CapaciteMaxTrains}" />
        </WrapPanel>
      </StackPanel>
    </GroupBox>

    <GroupBox Header="Trains de la station" Grid.Row="1" Padding="10">
      <StackPanel>
        <TextBlock Text="Tous les trains :" Style="{StaticResource LabelStyle}" Margin="0,0,0,5" />
        <ListBox ItemsSource="{Binding TrainsDeLaStation}" Height="120">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal">
                <TextBlock
                  Text="{Binding Nom}"
                  FontWeight="Bold"
                  Foreground="{StaticResource AccentBrush}"
                />
                <TextBlock Text=" - " />
                <TextBlock>
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Text" Value="{Binding Etat}" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding Etat}" Value="EnGare">
                          <Setter Property="Text" Value="🟢 En gare" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Etat}" Value="EnTransit">
                          <Setter Property="Text" Value="🚆 En transit" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Etat}" Value="EnAttente">
                          <Setter Property="Text" Value="⏳ En attente" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Etat}" Value="HorsService">
                          <Setter Property="Text" Value="❌ Hors service" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Etat}" Value="Programme">
                          <Setter Property="Text" Value="📅 Programmé" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock
          Text="Trains actuellement en gare :"
          Style="{StaticResource LabelStyle}"
          Margin="0,10,0,5"
        />
        <ListBox ItemsSource="{Binding TrainsEnGare}" Height="80">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="🟢 " Foreground="Green" />
                <TextBlock Text="{Binding Nom}" />
              </StackPanel>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </StackPanel>
    </GroupBox>
  </Grid>
</UserControl>
